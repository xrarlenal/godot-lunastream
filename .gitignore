# Zig
.zig-cache/
zig-out/
zig-pkg/

# Godot
# 用通配前缀：调试时把缓存挪成 .godot.bak1 这类名字，同样不该入库。
.godot*/
*.uid

# 构建产物
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.pdb

# 示例工程里随包安装的 Vulkan Layer（构建产物）。
# 源文件在 src/vulkan_layer/；这里被装上的是 .so 与清单的副本，两者同目录才能被
# 运行时找到（清单里的 library_path 是相对清单自己的），但都不该入库。
example/gdextension-smoke/addons/lunastream/luna_layer/

# macOS
.DS_Store

# 编辑器
.vscode/
.idea/
.zed/

# 打包产物（tools/package-addon.sh 的输出）与 LGPL FFmpeg 的构建目录
dist/

# 播放烟测拷进示例工程的片源（构建产物）
example/gdextension-smoke/clip*.mp4
