---
name: lunastream-dev-workflow
description: LunaStream 项目的开发作业指导——一个功能点怎么推进、提交要带什么、验证到什么程度、遇到问题怎么办。在本仓库做任何实现或修复工作时使用。
---

# LunaStream 开发作业指导

这份文件是**在本仓库做实现工作时必须遵守的规则**，由项目所有者逐条确定。
`docs/CONTRIBUTING.md` 面向人类贡献者讲目录与工具体；两者冲突时以本文件为准。

## 一、推进单位是"功能点"，不是"一批改动"

推进表在 `docs/ROADMAP.md`：**编号即顺序，也即提交序**。一个编号 = 一个功能点，
编号一旦使用不再复用。

- 按顺序做，不跳号。
- 每完成一个功能点，推进表对应行改成"✅ 已完成"，并把下一行标为"进行中"。
- 中途发现必须插入的新工作，**追加到表尾拿一个新编号**，并在功能文档里说明
  为什么它是计划外的（例：0022 就是做 0003 时暴露出来的）。

## 二、每个功能点必须交付三样，缺一不可

1. **实现代码**
2. **单元测试** —— 与代码同一次提交，不允许"先欠着测试"
3. **功能文档** `docs/features/NNNN-<slug>.md`，模板见 `docs/features/TEMPLATE.md`

功能文档要写清：这个功能点做了什么能力、为什么现在做、实现要点与取舍、
测试覆盖了什么行为、验证命令与实际结果、已知限制与下一步。

## 三、提交纪律

- **一次提交只做一件事。**
- **大功能点可以拆成多个自洽的提交**，每个提交本身必须完整（能编译、能验证），
  并在提交信息里标明是第几步，例如 `feat(ffsw): C ABI 契约头文件（0010 第 1 步）`。
  —— 不许把"没做完整个功能点"当成不动手的理由。
- 提交信息用 Conventional Commits 前缀 + 中文描述，scope 用目录名
  （`core` / `ffsw` / `ffvt` / `godot` / `vk` / `docs`）。

## 四、两条红线

| 红线 | 含义 |
|---|---|
| **不写未验证的结论** | 提交信息、功能文档里凡是"已验证/已通过"的表述，必须是真跑过那条命令得到的。没跑就写没跑。写错了要当场 amend 改正并说明。 |
| **不留下跑不起来的中间态** | 工作区要么干净，要么正在做一件马上能收尾的事。不留编译不过、测试不过的改动过夜；收不住就回退到上一个干净提交。 |

## 五、验证优先于功能量

- `src/core/` 是纯逻辑层：不依赖 Godot、不依赖 GPU、不依赖摄像头，
  **任何机器上 `zig build test` 就能验证**。凡是能被这层覆盖的逻辑，都先在这里做。
- **任何提交都必须让 `zig build test` 保持通过。**
- 需要引擎参与的验证（GDExtension 加载、出画）单独成步，不阻塞纯逻辑的推进。
- 破坏性/不可逆的实验（改扩展行为）之前，先想清楚怎么回退。

## 六、遇到问题的处理方式（项目所有者明确要求）

发现问题时，按这个顺序给结论：

1. **深度分析** —— 先定位，不要猜。用隔离实验把范围一步步缩窄。
2. **严重性评估** —— 明确它是"不安全 / 功能错误 / 资源泄漏 / 只是噪音"，
   并说明对真实使用方式的影响。
3. **解决方案** —— 给出可选方案与推荐项，说明各自代价。

### 三条被验证有效的调查方法

- **对照实验**：拿"已知能工作的对象"做同样操作。源工程的扩展多次充当对照组，
  直接证明某些现象是本项目引入的还是共有的。
- **探针必须固定指向同一个构建产物**。曾把探针跑在 `/tmp` 的工程副本上，
  而副本里的动态库冻在不同版本，导致两次同样的探测结果互相矛盾——结论一度作废。
- **剪枝实验**：把变量逐一加回或摘掉，而不是一次改一堆。

## 七、不要随意停止

没有真正的阻塞（推进不下去、需要新权限、需要用户决策）就不要停。判断"写不完"
时，把功能点拆小做完一步再停，而不是整体搁置——见第三条。

碰到已知的欠账（没修的 bug、没补的文档）要主动补，不留给后面。

## 八、项目边界

- 本仓库是**独立项目**：不复用、不写入其它项目的目录（尤其不要往
  `VideoAnd3DSceneFusion` 里写文件）。
- 源工程的代码是**复刻参考**，不是直接搬运的对象：逐功能点重写、重新验证。
  有意与源工程不同的地方，必须在功能文档里写明差异与理由。
- 插件定位：视频**源**，不是播放器。音频、字幕、seek、Android/iOS 都明确不做；
  平台优先级不含信创。

## 九、常用命令

```bash
# core 单元测试（不需要 Godot）
zig build test

# 构建 GDExtension 并装进示例工程（需要 Godot 可执行文件）
zig build -Dgodot-path="/Applications/Godot.app/Contents/MacOS/Godot"

# 引擎侧自检
"/Applications/Godot.app/Contents/MacOS/Godot" --headless --path example/gdextension-smoke --import
"/Applications/Godot.app/Contents/MacOS/Godot" --headless --path example/gdextension-smoke
```

`zig` 必须在 `PATH` 上——gdzig 的 bindgen 会 spawn `zig fmt`。

## 十、上下文余量的正确理解

对话很长时，工作记忆会接近装满，每一轮能写出的量变小。**这不是任务到顶，
也不意味着要新开会话**——上下文会自动压缩，带着摘要继续做。

应对方式就是本文件的原则本身：**小步、频繁提交、每一步自洽**。
对剩余余量的判断只是估算，偏保守时会显得过早停下——此时应当把功能点拆小
继续推进，而不是停下来解释。
